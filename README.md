# Cave

dev 
```bash
npm run dev
```

build 
```bash
npm run build
```

server 
```bash
npm install -g serve
serve -s dist -l 4000
```

npx json-server --host 192.168.1.1 --port 3004 --watch public/db/whiskies.json & npx serve -s dist -l 4000